1. Who was the last logged-in user?

index="t1110-003" winlog.event_id= "4624"
| sort - _time
| table _time , winlog.event_data.TargetUserName
=> Administrator

2. What is the logon type of the failed logons?

index="t1110-003" winlog.event_id= "4625"
| stats count by winlog.event_data.LogonType
=> LogonType: 3 with count 

3. What is the protocol the attacker tried to bruteforce?

=> index="t1110-003" 
| stats values(winlog.provider_name)
=> See Microsoft-Windows-TerminalServices-RemoteConnectionManager

=> index="t1110-003" winlog.provider_name="Microsoft-Windows-TerminalServices-RemoteConnectionManager"
| stats values(winlog.user_data.listenerName)

=> RDP-Tcp

4. How many users did the attacker succeed in getting their accounts?

https://www.socinvestigation.com/windows-rdp-event-ids-cheatsheet/
=> eventID 1149 (User Authentication Succeeded)
=> index="t1110-003" winlog.event_id=1149
| stats dc(winlog.user_data.Param1)

5. According to Microsoft. What is the description of the "Sub Status" code for event id 4625?
=> User logon with misspelled or bad password

6. How long did the bruteforce last? MM:SS

index="t1110-003" winlog.event_id=4625 "winlog.event_data.IpAddress"="192.168.1.60"
| stats earliest(_time) as first_time latest(_time) as last_time
| eval duration_sec = last_time - first_time

7. How many minutes passed before the attacker logged into the machine again?
timestamps in the results for my query in Q4.

8. What is the name of the policy used to lock the account after a certain number of failed login attempts?
=> Microsoft Learn. Using a search engine, I ran the query "microsoft learn account lock policy" to find the answer. => "Account Lockout"




